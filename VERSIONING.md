# バージョニング / ブランチ運用ルール

このフォーク（`exemoss/Unity_MultiTimelineRecorder`）のリリース番号とブランチの運用規約。
2026-08-06 制定。

## 前提: 何と互換なのかを「出力」で定義する

このパッケージの利用者は制作チームであり、消費するのは API ではなく **録画の設定と出力映像**。
そのため SemVer の「互換性」を、コードの互換ではなく次で定義する:

> **同じ録画設定のまま更新して、前と同じ絵が出るか**

納品プロジェクトでは「進行中の案件で更新して大丈夫か」がバージョン番号から読めることが最重要。

## 番号のルール（SemVer）

| | 意味 | 判定 |
|---|---|---|
| **MAJOR** | 既存の設定のまま**出力が変わる** / 設定が壊れる | 「前に書き出した素材と繋がらなくなる」なら major |
| **MINOR** | 機能追加。既存設定の出力は不変 | 新エンコーダ、新 UI、新項目 |
| **PATCH** | バグ修正のみ | 「設定どおりに直った」だけ |

### 判断に迷ったときの原則

- **番号は消耗品**。メジャーが増えるのを嫌って出力変化を patch に埋めない。
  番号を節約するより、更新の可否が一目で分かることの方が価値が高い。
- 出力が変わる変更は、CHANGELOG の `### Changed` に
  **旧バージョンとの差が具体的にどう出るか**（色/解像度/コーデック等）を必ず書く。

### 実例（このルールで格付けするとどうなるか）

| 変更 | 格付け | 理由 |
|---|---|---|
| NVENC の色を BT.601→BT.709 に修正（v1.5.22 相当） | **MAJOR** | 同じ設定で過去の書き出しと色が変わる |
| RT ソースの出力解像度を RT 実寸→Resolution 指定に変更（v1.5.25 相当） | **MAJOR** | 同じ設定で解像度が変わる |
| VP9 / ProRes エンコーダの追加 | MINOR | 既存設定の出力は不変 |
| Render History の追加 | MINOR | 録画結果に影響しない |
| VP9 の webm が 0 バイトになる不具合の修正 | PATCH | 設定どおりに直っただけ |

> v1.5.28 以前のタグは、このルール制定前のもの（すべて patch 運用）。
> 遡って振り直すことはしない（`Packages/manifest.json` から特定タグが参照されているため）。

## プレリリース

複数マシンでの検証が要る大きめの変更は、本リリース前に `-rc.N` を付けて配る。

```
v1.6.0-rc.1 → v1.6.0-rc.2 → v1.6.0
```

UPM は git タグをそのまま解決できるので、検証側は次のように参照する:

```json
"jp.iridescent.multitimelinerecorder":
  "https://github.com/exemoss/Unity_MultiTimelineRecorder.git?path=/jp.iridescent.multitimelinerecorder#v1.6.0-rc.1"
```

## package.json の version をいつ触るか

- **開発ブランチでは触らない。** リリース時にだけ確定させる。
- 理由: 開発ブランチが先に次の番号を名乗ると、リリースブランチをマージするたびに
  この 1 行だけがコンフリクトする（実際に v1.5.19〜28 の 10 リリースで毎回発生した）。
- リリース手順では「バージョン更新 + CHANGELOG」を独立した 1 コミットにする
  （`chore(package): bump version to X.Y.Z`）。

## ブランチ運用

- **`main` がトランク**。リリースタグは main 上で打つ。
- 作業は `feature/*` `fix/*` `chore/*` から分岐し、完了したら main へマージする。
- **マージ済みブランチは削除する**（内容は main に含まれるので失われない）。
  長期間放置された未マージブランチは、残す理由をブランチ名か PR に書く。

## リリース手順

1. main から作業ブランチを切る
2. 実装 + テスト（EditMode テストを通す）
3. 変更内容から上表で MAJOR / MINOR / PATCH を判定する
4. `package.json` の version 更新 + `CHANGELOG.md` 追記を 1 コミットで
5. main へマージ → `git tag -a vX.Y.Z` → `git push origin main vX.Y.Z`
6. 利用側の `Packages/manifest.json` の `#vX.Y.Z` を更新
